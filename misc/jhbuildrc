# -*- Mode: python -*- 
# Sample jhbuildrc file 
import os
moduleset = os.path.join(os.environ['HOME'], 'flu', 'prefix', 'flu.unstable.stable.modules')
modules = ['flumotion']
checkoutroot = os.path.join(os.environ['HOME'], 'flu', 'prefix', 'src')
prefix = os.path.join(os.environ['HOME'], 'flu', 'prefix')
