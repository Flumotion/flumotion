<registry>
  <components>
    <component type="tokentestbouncer"
               base="flumotion/component/bouncers"
               description="A test bouncer that only allows one token.">

      <source location="flumotion.component.bouncers.tokentest" />

      <entries>
        <entry type="component" location="tokentest.py"
               function="TokenTestBouncer" />
      </entries>

      <properties>
        <property name="authorized-token" type="string" required="True"
                  description="The token that is authorized" />
      </properties>
    </component>
  </components>
  <bundles>
    <bundle name="tokentest-base">
      <dependencies>
        <dependency name="component" />
      </dependencies>
      <directories>
        <directory name="flumotion/component/bouncers">
          <filename location="tokentest.py" />
        </directory>
      </directories>
    </bundle>
  </bundles>
</registry> 

