<registry>
  <components>
    <component type="http-streamer"
               base="flumotion/component/consumers/httpstreamer">
<!-- old way of specifying component source -->
      <source location="flumotion.component.consumers.httpstreamer.http"/>

      <eater name="default"/>

      <!-- entry points for distributable code bundles -->
      <entries>
        <entry type="component" location="http.py"
               function="MultifdSinkStreamer"/>
        <entry type="admin/gtk" location="admin_gtk.py"
               function="GUIClass"/>
      </entries>

      <sockets>
        <socket type="flumotion.component.plugs.loggers.Logger"/>
        <socket type="flumotion.component.plugs.streamdata.StreamDataProvider"/>
      </sockets>

      <properties>
        <property name="type" type="string" />
        <property name="bouncer" type="string" />
        <property name="issuer" type="string" />
        <property name="mount_point" type="string" />

        <!-- If type is 'master' (default) -->
        <property name="port" type="int" />

        <!-- If type is 'slave' -->
        <property name="porter_socket_path" type="string" />
        <property name="porter_username" type="string" />
        <property name="porter_password" type="string" />

        <!-- Hostname currently for display purposes -->
        <property name="hostname" type="string" />
        <property name="domain" type="string" />
        <property name="user_limit" type="int" />
        <property name="bandwidth_limit" type="int" />

        <!-- Burst on connect options. Defaults to off.
             If just burst_on_connect is specified, this gives a simple 
             burst-from-latest keyframe (so no effect for audio streams).

             Additionally, you MAY specify either (not both) of burst_size,
             burst_time. burst_size gives a minimum target burst in kB (it can
             be larger to start from a keyframe).
             burst_time gives a target burst time in seconds (but requires
             gst-plugins-base 0.10.11 or later).
          -->
        <property name="burst_on_connect" type="bool" />
        <property name="burst_size" type="int" />
        <property name="burst_time" type="float" />
      </properties>
    </component>
  </components>

  <bundles>

    <bundle name="httpstreamer-base">
        <dependencies>
            <dependency name="component-base" />
        </dependencies>

        <directories>
            <directory name="flumotion/component/consumers/httpstreamer">
                <filename location="__init__.py" />
            </directory>
        </directories>
    </bundle>

    <bundle name="httpstreamer-admin-gtk">
      <dependencies>
        <dependency name="httpstreamer-base" />
        <dependency name="base-admin-gtk" />
      </dependencies>

      <directories>
        <directory name="flumotion/component/consumers/httpstreamer">
          <filename location="http.glade" />
          <filename location="admin_gtk.py" />
        </directory>
      </directories>
    </bundle>

    <bundle name="httpstreamer-component">
        <dependencies>
            <dependency name="component" />
            <dependency name="httpstreamer-base" />
			<dependency name="porterclient" />
        </dependencies>

        <directories>
            <directory name="flumotion/component/consumers/httpstreamer">
                <filename location="http.py" />
                <filename location="resources.py" />
            </directory>
        </directories>
    </bundle>

  </bundles>
</registry> 
